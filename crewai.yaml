name: Run CrewAI Project

# Define the steps
steps:
  - name: Install Python dependencies
    command: pip install -r requirements.txt
    description: Install all required Python packages listed in requirements.txt

  - name: Run main.py
    command: python main.py
    description: Execute the main Python script